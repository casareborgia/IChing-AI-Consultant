# 주역 상담 AI - 아이디어 & 백로그 (BACKLOG)

기능 구현 아이디어 및 차후 고도화 항목을 기록하는 백로그 파일입니다. 스코프 확장은 코드에 직접 반영하기 전 먼저 이곳에 기록합니다.

## 💡 백로그 아이디어 목록

- [ ] **384효 번역 데이터 배치 생성**: LLM 또는 번역 API를 통해 괘사·효사 384건의 한국어 번역 텍스트 배치 생성
- [ ] **주자 『본의(本義)』 주석 데이터 추가 확보**: 정전(程傳) 외에 본의 데이터베이스 구성
- [ ] **HNSW 벡터 인덱스 튜닝**: `interpretation_chunks` 테이블의 embedding 컬럼에 `m=16`, `ef_construction=64` HNSW 인덱스 옵션 적용
- [ ] **Python 3.11+ 업그레이드 고려**: Vertex AI 최신 SDK와의 완벽 호환을 위해 Python 3.11 이상 환경으로 전환
- [ ] **로컬 임베딩으로 Google 의존 제거** (2026-08-16 기록)

      생성은 로컬로 옮기는 중인데 임베딩만 Vertex(`text-multilingual-embedding-002`)에
      남는다. 청크 2,536건이 그 모델로 적재돼 있어 질의도 같은 모델로 물어야 하기 때문이다.

      후보: `Qwen3-Embedding`(검색 전용, 다국어 강함) 또는 LM Studio에 이미 있는
      `nomic-embed-text-v1.5`. 후자는 768차원이라 컬럼과 그대로 맞지만 영어 중심이라
      한국어에는 불리하다. Qwen3 계열은 기본 차원이 더 크나 MRL로 768 절단이
      가능한 것으로 보인다 — **실제로 되는지 확인이 필요하다.**

      전제 조건: **한국어 검색 품질 검증이 먼저다.** 같은 질의 세트로 현재 결과와
      대조해 나빠지지 않음을 보이기 전에는 갈아타지 않는다. 검증 없이 바꾸면 RAG가
      조용히 나빠지고, 상담 근거가 얇아진 것을 한참 뒤에 알게 된다.

      작업량: 재임베딩 2,536건(로컬이면 무료), 768차원을 유지하면 스키마 변경 불필요.

      **참고 — 받아둔 Qwen3-TTS 계열로는 안 된다.** 음성 합성용이라 검색 벡터를
      뽑는 용도가 아니다. `Qwen3-Embedding`은 별도 모델이다.

## 안전 스크리너와 상담 마무리 신호가 같은 말을 두고 갈린다 (2026-08-16)

데모에서 정상 상담을 마치며 "정리가 좀 됐어요. 고맙습니다"라고 하자 위기 안내가
나갔다. 로컬 gemma4 기준이고 재현된다 — "정리가 됐어요" 단독은 BLOCK_CRISIS,
"고맙습니다" 단독은 NORMAL이다. 스크리너는 '정리'를 신변 정리로 읽는다.

문제는 이것이 우연이 아니라는 점이다. `prompts/counsel.md`의 마무리 신호 목록에
"정리가 됐어요"가 **예시로 들어 있다.** 앱이 마무리 신호로 가르치는 말을 스크리너가
위기 신호로 읽는다. 그대로 두면 상담을 잘 마친 사람이 자살예방 핫라인을 받고,
`CRISIS_LATCH_HOURS`(24시간) 동안 앱을 쓸 수 없다. 낙인이자 오작동이다.

시험 세트에 '정리' 사례가 셋 있는데(짐을 정리한다·다 정리했다·가게를 정리한다)
셋 다 신변·물건이다. **생각이 정리됐다**는 경우가 없어서 못 잡혔다.

손볼 자리: 스크리너 프롬프트에 '정리'의 목적어를 가르는 규칙(생각·마음이 정리
vs 신변·물건을 정리), 시험 세트에 성찰 마무리 사례 추가, 그 뒤 `score_safety.py`
재채점. 안전 판정을 건드리는 일이라 숫자를 다시 내기 전에는 손대지 않는다.
