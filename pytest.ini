[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -m "not live_api"
markers =
    live_api: 실제 외부 API를 호출하는 명시적 opt-in 테스트
    destructive_db: 전용 PostgreSQL 테스트 DB의 데이터를 삭제하거나 DDL을 실행하는 테스트
